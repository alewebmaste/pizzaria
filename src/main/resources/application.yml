spring:
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: none
  
  datasource:
    url: jdbc:mysql://localhost:3306/pizzaria
    username: root
    password: paulistano@13
    driver-class-name: com.mysql.jdbc.Driver

server:
  servlet:
    context-path: /api/v1
  port: 8080